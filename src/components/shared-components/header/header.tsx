import React from 'react';
import * as Styled from './styled';

const Header: React.FC = () => {

    return (
        <div>
            <Styled.Header>
                Trello Clone
            </Styled.Header>
        </div>
    );

};

export default Header;